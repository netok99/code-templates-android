#if (${PACKAGE_NAME} &amp;&amp; ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import android.content.Context;
import android.support.v7.widget.RecyclerView;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;
import java.util.List;

#parse("File Header.java")
public class ${NAME_CLASS_ADAPTER} extends RecyclerView.Adapter {
    private ${ITEMS_NAME_CLASS} ${ITEMS_NAME};
   
    public ${NAME_CLASS_ADAPTER}(${ITEM_CLASS} ${ITEMS_NAME}) {
        this.${ITEMS_NAME} = ${ITEMS_NAME};
    }

    @Override
    public RecyclerView.ViewHolder onCreateViewHolder(ViewGroup parent, int viewType) {
        return new ${VIEWHOLDER_CLASS}(
            DataBindingUtil.inflate(
                LayoutInflater.from(parent.getContext()), R.layout.${LAYOUT_RES_ID}, parent, false
            );
        );
    }

    @Override
    public void onBindViewHolder(RecyclerView.ViewHolder holder, int position) {
        final ${ITEMS_NAME_CLASS} item = ${ITEMS_NAME}.get(position);
        ${NAME_VIEWHOLDER_CLASS} viewHolder = (${NAME_VIEWHOLDER_CLASS}) holder;
        viewHolder.binding.${NAME_VARIABLE_BINDING}(item);
        //TODO Fill in your logic for binding the view.
        viewHolder.binding.executePendingBindings();
    }

    @Override
    public int getItemCount() {
        if (${ITEMS_CLASS} == null)
            return 0;
        return ${ITEMS_CLASS}.size();
    }

    public class ${NAME_VIEWHOLDER_CLASS} extends RecyclerView.ViewHolder {
        ${BINDING_CLASS} binding;

        public ${NAME_VIEWHOLDER_CLASS}(${BINDING_CLASS} binding) {
            super(binding.getRoot());
            this.binding = binding;
        }
    }
}
